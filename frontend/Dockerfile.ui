# Dockerfile.ui — Vite/React dev server
FROM node:20-alpine

WORKDIR /app

# Copy package files first for layer caching
COPY package.json package-lock.json* ./

RUN npm install

# Copy source (overridden by volume mount in docker-compose dev)
COPY . .

EXPOSE 5173
