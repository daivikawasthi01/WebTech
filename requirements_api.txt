# API server dependencies (add to existing requirements.txt env)
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.0,<3.0

# Optional — comment out if disk space is tight:
# shap>=0.45,<1.0          # SHAP feature importance (requires torch)
